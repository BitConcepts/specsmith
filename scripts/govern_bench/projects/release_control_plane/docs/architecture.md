# Release Control Plane Architecture

## Components

- Python API — health endpoint only in the starter.
- Go worker — release-plan normalizer placeholder.
- React UI — static operator shell.
- JSON Schema — incomplete shared release contract.

## T29 decision record

Document the completed plan-to-approval data flow, contract ownership, failure
handling, and why the benchmark keeps release state in memory.
